packages:
  - apps/*
  - packages/*

# esbuild (pulled in by vite/vitest) needs its postinstall to fetch the native binary.
allowBuilds:
  better-sqlite3: true
  esbuild: true

# This repo opts out of the global new-release quarantine so pinned toolchain
# versions install reproducibly.
minimumReleaseAge: 0
